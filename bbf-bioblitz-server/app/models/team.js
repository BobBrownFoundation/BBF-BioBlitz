import { Model } from 'lux-framework';

class Team extends Model {
  /*static hasMany = {
    participants: {
      inverse: 'teams',
      through: 'team-participants'
    }
  };*/
}

export default Team;
